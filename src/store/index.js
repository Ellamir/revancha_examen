import Vue from 'vue'
import Vuex from 'vuex'
//import {apiCall, apiLogin} from '@/config/apiConfig'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
